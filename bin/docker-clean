#!/usr/bin/env zsh

IDS=`docker ps -a -q -f status=exited`
if [[ ! -z ${IDS} ]]; then
	docker rm -v ${IDS}
fi

IMAGES=`docker images -f "dangling=true" -q`
if [[ ! -z ${IMAGES} ]]; then
	docker rmi `${IMAGES}`
fi

# Local Variables:
# mode: sh
# End:

#!/usr/bin/env python3

import errno
import os
import sys

def main():
     errcode = int(sys.argv[1])
     print(errno.errorcode[errcode], os.strerror(errcode))

if __name__ == '__main__':
    main()
